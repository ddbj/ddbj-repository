# locals: (submission_request:, unread_request_ids: Set.new)

submission  = submission_request.submission
accessions  = submission ? submission.accessions.to_a : []

{
  **submission_request.slice(:id, :db, :status, :created_at),

  submission_id:              submission&.id,
  source_id:                  submission&.source_id,
  first_accession:            accessions.min_by(&:id)&.number,
  accession_count:            accessions.size,
  has_unread_curator_message: unread_request_ids.include?(submission_request.id)
}
