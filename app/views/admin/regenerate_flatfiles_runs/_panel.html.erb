<%# A run, reading either as progress or as a result. One partial for %>
<%# both so the tool screen and the run's own page cannot drift. %>
<%= render "admin/regenerate_flatfiles_runs/#{run.loading? ? 'progress' : 'result'}", run: %>
