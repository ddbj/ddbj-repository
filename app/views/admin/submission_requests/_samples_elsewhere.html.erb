<%# Said in two places — beside a truncated samples table, and on a record
    too large to lay one out — so it is written once. %>
The whole list of samples is on the
<%= link_to 'Samples', samples_admin_submission_request_path(@request) %>
tab, paginated and searchable.
