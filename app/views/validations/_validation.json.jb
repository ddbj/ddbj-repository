{
  **validation.slice(
    :id,
    :progress,
    :created_at,
    :finished_at,
    :validity
  ),

  details: validation.details.map {|detail|
    detail.slice(
      :entry_id,
      :code,
      :severity,
      :message
    )
  }
}
