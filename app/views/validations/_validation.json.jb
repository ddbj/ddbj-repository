{
  **validation.slice(
    :id,
    :progress,
    :created_at,
    :finished_at,
    :validity
  ),

  details: validation.details.map {|detail|
    detail.slice(
      :filename,
      :entry_id,
      :code,
      :severity,
      :message
    )
  }
}
