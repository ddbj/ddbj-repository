# locals: (accession:)

{
  **accession.slice(
    :number,
    :entry_id,
    :version,
    :last_updated_at
  ),

  renewals: render(partial: 'accession_renewals/accession_renewal', collection: accession.renewals)
}
